#include "../push_swap.h"

void	sort(int *list)
{
	// printf("%lu\n", sizeof(int));
	printf("%d\n", list[1]);
	// size_t i;
	// size_t length;

	// i = 0;
	// length = sizeof(list);
	// while (i < length / sizeof(int))
	// {
	// 	printf("%d\n", list[i]);
	// 	i++;
	// }
}